spring:
  datasource:
    url: jdbc:mysql://localhost:3306/enterpriseflow_prod
    username: root
    password: asif
  jpa:
    hibernate:
      ddl-auto: validate   # production me table auto change nahi hoga
    show-sql: false
logging:
  level:
    root: ERROR