spring:
  datasource:
    url: jdbc:mysql://localhost:3306/enterpriseflow_dev
    username: root
    password: root
  jpa:
    hibernate:
      ddl-auto: update   # dev me table auto update hone denge
    show-sql: true
    properties:
      hibernate:
        format_sql: true
